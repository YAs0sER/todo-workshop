const tasks = [];


//form event Listener
const addForm = document.getElementById("add");
addForm.addEventListener('submit', (e)=>{
 e.preventDefault(); 
})
funtion getTitle(){
  const 
}
function addTask(title){
  
}